
import { setStrategy, modifyStrategy } from './setStrategy';
import authenticate from './authenticate';
import { login, logout, init } from './login';
import { checkLogged, checkNotLogged } from './checkLogged';

export {
  authenticate, setStrategy, modifyStrategy, login, logout, init, checkLogged, checkNotLogged
};
